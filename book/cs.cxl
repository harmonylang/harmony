def process(self):
    while True:
        #enter critical section
        @cs: assert (atLabel("cs")) == dict{ nametag(): 1 }, atLabel("cs");
        #exit critical section
    ;
;
spawn process(0), 0;
spawn process(1), 1;
