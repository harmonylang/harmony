def incrementer(self):
    count = count + 1;
    done[self] = True;
;
def main(self):
    while not (done[0] and done[1]):
        pass;
    ;
    assert count == 2, count;
;
count = 0;
done = [ False, False ];
spawn incrementer(0), 0;
spawn incrementer(1), 1;
spawn main();
