def process(self):
    while True:
        @ncs: pass;
        #enter critical section
        @cs: assert (atLabel "cs") == dict{ nametag(): 1 };
        #exit critical section
    ;
;
spawn process(0), 0;
spawn process(1), 1;
