def thread(self):
    while choose({ False, True }):
        #enter critical section
        @cs: assert atLabel(cs) == bag{ (thread, self) }
        #exit critical section
    
spawn thread(0)
spawn thread(1)
