def setX(a):
    x = a;
    y = 100 - a;
;
def getXY():
    result = [x, y];
;
def checker():
    let xy = getXY():
        assert (xy[0] + xy[1]) == 100, xy;
    ;
;
x = 0;
y = 100;
spawn checker();
spawn setX(50);
