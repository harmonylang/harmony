def thread():
    while True:
        # Enter critical section

        # Critical section is here
        @cs: assert countLabel(cs) == 1

        # Exit critical section
    
spawn thread()
spawn thread()
