import abp;

const NMSGS = 5;

def sender():
    for i in {1..NMSGS}:
        app_send(i);
    ;
;
def receiver():
    for i in {1..NMSGS}:
        let payload = app_recv(i == NMSGS):
            assert payload == i;
        ;
    ;
;

spawn sender();
spawn receiver();
