count = 0;
done = [ False, False ];

def incrementer(self):
    count = count + 1;
    done[self] = True;
;
def main():
    await all(done);
    assert count == 2, count;
;

spawn incrementer(0);
spawn incrementer(1);
spawn main();
