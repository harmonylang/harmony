import synch;
import alloc;

def lst_node(v, n):
    result = malloc();
    !result = dict{ .lock: Lock(), .value: v, .next: n };
;
def lst_contains(lst, v):
    let n = lst:
        while (!n).value < v:
            n = (!n).next;
        ;
        result = (!n).value == v;
    ;
;
