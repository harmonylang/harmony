import alloc;

def Stack():
    result = None;
;
def stack_push(st, v):
    !st = malloc(dict{ .value: v, .rest: !st });
;
def stack_pop(st):
    let node = !st:
        result = node->value;
        !st = node->rest;
        free(node);
    ;
;
