import list

def Queue() returns empty:
    empty = []

def put(q, v):
    !q = list.append(!q, v)

def get(q) returns next:
    if !q == []:
        next = None
    else:
        next = list.head(!q)
        !q = list.tail(!q)

