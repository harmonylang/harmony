def Semaphore(cnt):
    result = cnt

def P(sema):
    let blocked = True:
        while blocked:
            atomic:
                if (!sema) > 0:
                    !sema -= 1
                    blocked = False

def V(sema):
    atomic:
        !sema += 1
