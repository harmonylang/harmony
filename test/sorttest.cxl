import list;

const N = 4;

def sorted(a):
    result = True;
    for i in 1..(len(a) - 1):
        if a[i - 1] > a[i]:
            result = False;
        ;
    ;
;
const values = 1..N;
list = [ choose(values) for i in 1..choose(0..N) ];
s = qsort(list);
assert sorted(s);
assert list2bag(list) == list2bag(s);
