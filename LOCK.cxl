# mutual exclusion using ordinary locks

import synch;

def p():
    while True:
        call lock&(mylock);
        @cs: atomic: pass;;   # critical section
        call unlock&(mylock);
    ;
;
mylock = Lock();
atomic:
    spawn p(), 0;
    spawn p(), 0;
    spawn p(), 1;
    spawn p(), 1;
;
