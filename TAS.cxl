import synch;

def p():
    while True:
        while tas&(mylock):
            pass;
        ;
        @cs:    # critical section
        mylock = False;
    ;
;
mylock = Lock();
spawn p(), 0;
spawn p(), 1;
